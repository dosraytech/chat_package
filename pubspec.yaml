name: chat_package
description: Fully functional chat ui for flutter, with functionality to send
  text, voice notes,and images
version: 2.0.1
homepage: https://github.com/MOUKZ/chat_package

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  intl: ^0.19.0
  just_audio: ^0.9.3
  path_provider: ^2.1.5
  permission_handler: ^12.0.0+1
  photo_view: ^0.15.0
  provider: ^6.1.5
  record: ^6.0.0
  voice_message_package: ^2.2.1

dev_dependencies:
  build_runner: ^2.3.3
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter: null
